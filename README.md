# ClariMeet — Step 1

Minimal scaffold with a tiny CLI:
- `clarimeet hello` — sanity check
- `clarimeet probe <audio.wav>` — prints WAV metadata (stdlib only)

Use the `src/` layout and install in editable mode to avoid import errors.
